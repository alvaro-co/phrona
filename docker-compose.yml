services:
  phrona:
    build: .
    image: phrona/phrona:0.2.0
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      PHRONA_SERVER_BIND_ADDR: "0.0.0.0:8080"
      PHRONA_API_KEY: ${PHRONA_API_KEY:-}
